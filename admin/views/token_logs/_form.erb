<% error = @token_log.errors.key?(:user_id) && @token_log.errors[:user_id].count > 0 %>
<fieldset class='control-group <%= error ? 'has-error' : ''%>'>
  <%= f.label :user_id, :class => 'control-label' %>
  <div class='controls'>
    <%= f.text_field :user_id, :class => 'form-control input-xlarge input-with-feedback', :autofocus => true %>
    <span class='help-inline'><%= error ? f.error_message_on(:user_id) : "Ex: a simple text" %></span>
  </div>
</fieldset>
<% error = @token_log.errors.key?(:content) && @token_log.errors[:content].count > 0 %>
<fieldset class='control-group <%= error ? 'has-error' : ''%>'>
  <%= f.label :content, :class => 'control-label' %>
  <div class='controls'>
    <%= f.text_field :content, :class => 'form-control input-xlarge input-with-feedback' %>
    <span class='help-inline'><%= error ? f.error_message_on(:content) : "Ex: a simple text" %></span>
  </div>
</fieldset>
<% error = @token_log.errors.key?(:token) && @token_log.errors[:token].count > 0 %>
<fieldset class='control-group <%= error ? 'has-error' : ''%>'>
  <%= f.label :token, :class => 'control-label' %>
  <div class='controls'>
    <%= f.text_field :token, :class => 'form-control input-xlarge input-with-feedback' %>
    <span class='help-inline'><%= error ? f.error_message_on(:token) : "Ex: a simple text" %></span>
  </div>
</fieldset>
<% error = @token_log.errors.key?(:before_token) && @token_log.errors[:before_token].count > 0 %>
<fieldset class='control-group <%= error ? 'has-error' : ''%>'>
  <%= f.label :before_token, :class => 'control-label' %>
  <div class='controls'>
    <%= f.text_field :before_token, :class => 'form-control input-xlarge input-with-feedback' %>
    <span class='help-inline'><%= error ? f.error_message_on(:before_token) : "Ex: a simple text" %></span>
  </div>
</fieldset>
<% error = @token_log.errors.key?(:after_token) && @token_log.errors[:after_token].count > 0 %>
<fieldset class='control-group <%= error ? 'has-error' : ''%>'>
  <%= f.label :after_token, :class => 'control-label' %>
  <div class='controls'>
    <%= f.text_field :after_token, :class => 'form-control input-xlarge input-with-feedback' %>
    <span class='help-inline'><%= error ? f.error_message_on(:after_token) : "Ex: a simple text" %></span>
  </div>
</fieldset>


<div class="form-actions">
  <%= f.submit pat(:save), :class => 'btn btn-primary' %>
  &nbsp;
  <%= f.submit pat(:save_and_continue), :class => 'btn btn-info', :name => 'save_and_continue' %>
  &nbsp;
  <%= link_to pat(:cancel), url(:token_logs, :index), :class => 'btn btn-default' %>
</div>